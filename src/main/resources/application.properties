spring.application.name=springbootfirst

# Server port (optional: change if default 8080 is busy)
# server.port=8081

# ? Database Configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://yamabiko.proxy.rlwy.net:34378/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=kpGyMyjyZANBKMuqRiUuVQbgGcPSGHRT

# ? Hibernate / JPA Settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? JWT Configuration
app.jwt-secret=ThisIsMyVerySecureAndLongJwtSecretUsedOnlyForSigningTokensInSpringBootAndForItsSecurity!
app.jwt-expiration-milliseconds=36000000

# ? Security (Commented out; fine for now)
# spring.security.user.name=root
# spring.security.user.password=root1
# spring.security.user.roles=ADMIN,USER,GUEST
